Objecte de la prova: En aquesta prova es comprova el descomprimir() de la classe Comprimit.

Drivers: Evidentment, com el nom del executable ja ens indica, el DriverClasseComprimit.jar es un executable del driver de la classe Comprimit.

Fitxers necessaris: Tots els fitxers necessaris per la prova es troben al mateix directori que el del .jar, per la comoditat del qui ho hagui de provar, aixi doncs, no cal modificar res, nomes executar el .jar.

Valors estudiats i efectes estudiats: Estudiem el comportament de la classe que hem programat en fitxer arbitraris. El nostre objectiu es veure si la classe es comporta igual de be amb diferents tipus de fitxers, i si no fos aixi, intentar entendre perque en base al fitxer que falla.

Operativa: Executar el .jar, i triar una de les 3 opcions, introduint el nombre corresponent. A continuacio, introduir les dades que necessiti. En el cas de descomprimir s’ha d’indicar el path del fitxer a descomprimir, el nom de l’arxiu de sortida i el path del fitxer de sortida.


